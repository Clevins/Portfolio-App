export default class CPortfolio {
  name: string;
  link: string;
  image: string;

  constructor(name: string, link: string, image: string) {
    this.name = name;
    this.link = link;
    this.image = image;
  }
}
